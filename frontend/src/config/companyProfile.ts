/** Placeholder until settings/company-profile backend exists. */
export const companyProfile = {
  name: 'Your Company Name',
  phone: null as string | null,
  locations: [] as string[],
  logoUrl: null as string | null,
}
